const { init } = require('./base');
const { scrap_players } = require('./players.js');
const { scrap_items } = require('./items.js');
const { scrap_stages } = require('./stages.js');

module.exports = {
	init,
	scrap_players,
	scrap_items,
	scrap_stages,
};
